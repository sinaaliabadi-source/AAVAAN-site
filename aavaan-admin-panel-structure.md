# ساختار پنل مدیریت آوان (Admin Panel)

این سند ادامه‌ی مستقیم `aavaan-site-structure.md` و `claude-code-advanced-profile-prompt.md` است. هدف: طراحی کامل پنل مدیریت آوان، شامل فهرست اولیه‌ی سینا + موارد تکمیلی توافق‌شده در گفتگو، به‌علاوه فازبندی اجرا و پرامپت Claude Code برای فاز اول.

---

## ۰. زمینه‌ی فنی فعلی پروژه (برای Claude Code)

- پروژه روی **Laravel 12.x / PHP 8.2.x** اجرا می‌شود (هاست ارتقا یافت؛ نیازی به سازگاری با PHP 8.1 یا ساختار Laravel 10 نیست — هر اشاره‌ی قدیمی به داون‌گرید در اسناد قبلی منسوخ شده است).
- دیتابیس: MariaDB 10.6.24، هاست DirectAdmin (irwebspace)، **بدون دسترسی SSH** — همان پایپ‌لاین قبلی: Claude Code محلی → حسابرسی Claude → خروجی SQL/فایل → آپلود FTP و ایمپورت phpMyAdmin توسط سینا.
- مدل `User` از قبل ستون `role` دارد (مقادیر از جمله `admin`) — یعنی تشخیص ادمین از پایه وجود دارد، فقط پنلی برایش ساخته نشده.
- جدول `verifications` از فاز «پروفایل پیشرفته» از قبل وجود دارد ولی **بدون رابط کاربری** — تأیید هویت فعلاً فقط از طریق phpMyAdmin دستی انجام می‌شود.
- برند: پس‌زمینه عاجی `#F6F1E7`، لاجوردی `#1F2A44`، طلایی دودی `#C9A24B`؛ فونت عنوان `YekanBakh-VF.ttf`، فونت بدنه `IRANSansXV.woff2` — پنل ادمین هم باید از همین هویت بصری استفاده کند (نیازی نیست luxury باشد، ولی نباید قالب پیش‌فرض Bootstrap/AdminLTE بی‌ربط به برند باشد).
- آپلود فایل (در صورت نیاز در فازهای بعدی مثل بنر) باید از همان الگوی حل‌شده‌ی قبلی استفاده کند: مسیر واقعی از طریق `$_SERVER['DOCUMENT_ROOT']` محاسبه شود، نه `public_path()` خام (به‌خاطر mismatch بین ریشه‌ی Laravel و ریشه‌ی سرویس‌دهی روی این هاست).

### ⚠️ نکته‌ی مهم درباره‌ی وابستگی به مدل داده

بخشی از فهرست زیر (مدیریت **پروژه‌ها**، **درخواست‌ها/Applications**، **استخدام‌های موفق**، **سیستم داوری اختلاف**، **قرارداد دیجیتال**) به جداولی نیاز دارد که **هنوز در دیتابیس وجود ندارند**. مدل فعلی آوان بر اساس «اشتراک ثابت برای هنرمند + پرداخت به‌ازای دسترسی برای تیم تولید» است، نه یک مدل آگهی‌پروژه/درخواست/استخدام به سبک سایت‌های فریلنسری.

این یعنی پیش از ساخت پنل مدیریتیِ آن بخش‌ها، باید یک **فاز پیش‌نیاز مستقل** برای طراحی مدل داده‌ی پروژه/درخواست/استخدام/اختلاف نوشته شود (شبیه فاز ۱ پروفایل پیشرفته که قبل از فرم و نمایش، schema را ساخت). این سند آن فاز پیش‌نیاز را به‌عنوان «فاز پایانی» مشخص کرده، اما طراحی دقیق جداولش را به یک سند جداگانه موکول می‌کند تا حجم این سند کنترل‌شده بماند.

بخش‌هایی که **روی جداول موجود** (`users`, `artist_profile_premium`, `verifications`, اشتراک/پرداخت) قابل‌ساخت‌اند، اولویت فازهای ابتدایی هستند.

---

## ۱. سطوح دسترسی ادمین (RBAC) — جدید

پنل ادمین چون اقدامات حساس زیادی دارد (مسدودسازی، استرداد وجه، تأیید هویت، رأی در اختلاف)، باید از همان ابتدا چند سطح دسترسی داشته باشد:

| نقش | دسترسی |
|---|---|
| ادمین کل (Super Admin) | همه‌چیز، شامل مدیریت نقش‌های ادمین دیگر |
| پشتیبانی (Support) | کاربران، تیکتینگ، گزارش‌های تخلف |
| مالی (Finance) | پرداخت‌ها، استرداد وجه، پلن‌ها و کدهای تخفیف |
| ناظر محتوا (Content Moderator) | صف بررسی محتوا، مدیریت محتوای سایت/بلاگ |

- لاگ مجزای اقدامات ادمین (جدا از لاگ فعالیت عمومی کاربران): چه ادمینی، چه کاری، روی چه رکوردی، چه زمانی.

---

## ۲. تنظیمات عمومی سیستم — جدید

- اطلاعات تماس و شبکه‌های اجتماعی (نمایش در فوتر/صفحه تماس)
- متاتگ‌های سئوی هر صفحه (عنوان/توضیح پیش‌فرض)
- نرخ کارمزد پلتفرم در صورت وجود کمیسیون (قابل تنظیم، نه هاردکد در کد)
- مدیریت قالب ایمیل/پیامک (متن پایه‌ی اعلان‌ها)
- **توجه امنیتی:** کلیدهای حساس درگاه پرداخت (زرین‌پال) از طریق این رابط وب قابل‌ویرایش نباشند؛ این‌ها فقط در `.env` می‌مانند. تنظیمات این بخش فقط شامل مقادیر غیرحساس عملیاتی است.

---

## ۳. نمای کلی (Dashboard Overview)

از فهرست اصلی سینا — قابل‌ساخت روی جداول موجود:

- تعداد کاربران ثبت‌نام‌شده / تعداد هنرمندان / تعداد کارفرمایان
- تعداد اشتراک‌های فعال
- درآمد ماه جاری
- کاربران جدید امروز/هفته

موارد زیر **به مدل داده‌ی پروژه/استخدام نیاز دارند** (فاز پایانی، نه فاز اول):
- تعداد پروژه‌های فعال، تعداد درخواست‌های همکاری، تعداد استخدام‌های موفق، نرخ تبدیل ثبت‌نام به همکاری

---

## ۴. گزارش‌گیری و خروجی پیشرفته — جدید

مکمل بخش «نمای کلی»، نه جایگزین آن:

- گزارش با بازه‌ی زمانی دلخواه (نه فقط «ماه جاری»)
- خروجی Excel/CSV از لیست کاربران، تراکنش‌ها، اشتراک‌ها
- گزارش درآمد دوره‌ای (هفتگی/ماهانه/سالانه)
- قیف تبدیل: ثبت‌نام → تکمیل پروفایل → خرید اشتراک (و بعداً، وقتی مدل استخدام ساخته شد: → استخدام موفق)

---

## ۵. مدیریت کاربران

### هنرمندان
- مشاهده پروفایل
- ویرایش اطلاعات
- مسدودسازی
- تأیید هویت (روی جدول `verifications` موجود — این فقط نیاز به رابط کاربری دارد، نه جدول جدید)
- مشاهده سوابق فعالیت
- مشاهده رزومه و نمونه‌کار

### کارفرمایان (تیم‌های تولید)
- مشاهده اطلاعات شرکت یا تیم
- تأیید هویت
- مسدودسازی
- مشاهده تاریخچه‌ی دسترسی‌های خریداری‌شده (معادل فعلی «پروژه‌های ثبت‌شده» تا قبل از ساخت مدل پروژه)

---

## ۶. صف بررسی محتوا (Moderation Queue) — جدید

با «تأیید تخصص» (بخش ۱۲) فرق دارد: آن رزومه/مهارت را تأیید می‌کند، این محتوای تصویری/متنی را پیش از انتشار عمومی از نظر مناسب‌بودن بررسی می‌کند.

- صف عکس‌های پروفایل/نمونه‌کار تازه‌آپلودشده، در انتظار تأیید قبل از نمایش عمومی
- تأیید / رد با دلیل
- (اختیاری فاز بعد) بررسی متن بیوگرافی برای محتوای نامناسب

---

## ۷. مدیریت گزارش‌های تخلف کاربران

اگر کاربری توسط کاربر دیگر گزارش شود:

- مشاهده گزارش
- بررسی مدارک
- اخطار
- تعلیق
- بستن پرونده

---

## ۸. مدیریت محتوا (CMS)

- صفحات سایت (درباره، چگونه‌کار‌می‌کند، و...)
- سوالات متداول
- قوانین و حریم خصوصی
- بلاگ
- بنرها

---

## ۹. مدیریت پلن‌های اشتراک

- پلن رایگان (محدودیت‌ها)
- پلن حرفه‌ای (امکانات فعال)
- پلن ویژه (امکانات فعال)
- ایجاد پلن جدید، تغییر قیمت، فعال/غیرفعال‌سازی
- **کد تخفیف/کوپن (جدید):** کد با درصد یا مبلغ تخفیف ثابت، تاریخ انقضا، سقف تعداد استفاده، محدود به پلن خاص یا همه‌ی پلن‌ها

---

## ۱۰. مدیریت پرداخت‌ها

- مشاهده تراکنش‌ها
- وضعیت پرداخت
- استرداد وجه
- فاکتورها

---

## ۱۱. مدیریت پروفایل‌های ویژه

مثل Featured Artist، پروفایل طلایی، نمایش اولویت‌دار:

- چه کسی خریده
- تا چه زمانی اعتبار دارد
- تمدید شده یا نه

---

## ۱۲. سیستم تأیید تخصص

روی جدول `verifications` موجود — این فاز فقط رابط کاربری مدیریتی برایش می‌سازد (تا الان فقط از phpMyAdmin قابل تغییر بوده):

- بررسی رزومه
- بررسی نمونه‌کار
- تأیید مهارت (مثلاً «بازیگر تأیید‌شده»، «صدابردار تأیید‌شده»)

---

## ۱۳. سیستم امتیازدهی و اعتماد

برای هر کاربر:
- امتیاز کیفیت
- تعداد پروژه موفق
- نرخ پاسخگویی
- میزان لغو همکاری

> وابسته به مدل پروژه/استخدام — فاز پایانی.

---

## ۱۴. مدیریت هوشمند Matching

مدیر بتواند ببیند:
- چه پروژه‌هایی متقاضی کم دارند
- چه تخصص‌هایی کمبود دارند
- چه شهرهایی عرضه کم دارند
- چه تخصص‌هایی بیشترین تقاضا را دارند

> وابسته به مدل پروژه/درخواست — فاز پایانی.

---

## ۱۵. سیستم داوری و حل اختلاف

اگر بین طرفین اختلاف ایجاد شد:
- ثبت پرونده
- مستندات
- رأی مدیر
- تاریخچه

**قرارداد دیجیتال (جدید، توافق‌شده):** وقتی «استخدام موفق» ثبت می‌شود، یک توافق‌نامه‌ی ساده‌ی دیجیتال بین هنرمند و کارفرما تولید و آرشیو شود — مرجع مستند برای حل اختلاف بعدی.

> وابسته به مدل پروژه/استخدام — فاز پایانی.

---

## ۱۶. تیکتینگ پشتیبانی — جدید (تکمیل‌شده طبق نظر سینا)

با مدیریت گزارش‌های تخلف (بخش ۷) فرق دارد: آن شکایتِ کاربر از کاربر است، این کانال «من مشکل فنی دارم / سوال دارم» است.

- **سمت کاربر:** بخش «پشتیبانی من» در پنل هنرمند و پنل کارفرما برای باز‌کردن تیکت و دیدن پاسخ‌ها
- **سمت ادمین:** صف تیکت‌ها، ارجاع به نقش مربوطه، پاسخ، تغییر وضعیت (باز/در حال بررسی/بسته)

---

## ۱۷. ابزارهای مدیریتی حرفه‌ای

### جستجوی سراسری
مدیر فقط نام را وارد کند و پیدا کند: کاربر، تراکنش (و بعداً پروژه/درخواست). طراحی باید قابل‌توسعه باشد — اضافه‌شدن «پروژه» به نتایج جستجو در فاز پایانی نباید نیاز به بازنویسی موتور جستجو داشته باشد.

### لاگ فعالیت
ثبت اقدامات کاربران عادی (ورود، تغییر اطلاعات، پرداخت، حذف) — جدا از لاگ فعالیت ادمین در بخش ۱.

### سیستم اعلان
ارسال پیامک/ایمیل/نوتیفیکیشن داخل سایت، به‌صورت فردی/گروهی/همگانی.

---

## ۱۸. پشتیبان‌گیری از دیتابیس — جدید

چون SSH وجود ندارد، یک دکمه‌ی «دانلود بکاپ دیتابیس» داخل پنل ادمین (خروجی SQL قابل دانلود) — جایگزین کرون‌جاب معمول که در این هاست در دسترس نیست.

---

## ۱۹. موارد بررسی‌شده و کنارگذاشته‌شده (برای مرجع آینده، دوباره مطرح نشود مگر شرایط عوض شود)

- **اتصال به سامانه مودیان:** فعلاً لازم نیست.
- **ورود دو مرحله‌ای (2FA) برای ادمین:** فعلاً لازم نیست.

---

## ۲۰. فازبندی پیشنهادی اجرا

طبق همان روال قبلی (یک پرامپت در هر مرحله، تست محلی بین مراحل):

| فاز | محتوا | وابستگی |
|---|---|---|
| **فاز ۱** | زیرساخت: RBAC، احراز هویت ادمین، layout پنل، تنظیمات عمومی، نمای کلی (آمارهای قابل‌محاسبه فعلی)، لاگ فعالیت ادمین، اسکلت جستجوی سراسری | جداول موجود |
| فاز ۲ | مدیریت کاربران (هنرمند/کارفرما) + تأیید تخصص (رابط `verifications`) | جداول موجود |
| فاز ۳ | پلن‌های اشتراک + کد تخفیف + پرداخت‌ها/استرداد + پروفایل‌های ویژه | جداول موجود |
| فاز ۴ | صف بررسی محتوا + مدیریت گزارش‌های تخلف | جداول موجود |
| فاز ۵ | مدیریت محتوای سایت (CMS) | جداول موجود |
| فاز ۶ | تیکتینگ پشتیبانی (ادمین + کاربر) + سیستم اعلان | جداول موجود |
| فاز ۷ | پشتیبان‌گیری از دیتابیس + گزارش‌گیری/خروجی پیشرفته | جداول موجود |
| **فاز پیش‌نیاز** | طراحی مدل داده‌ی پروژه/درخواست/استخدام/اختلاف (سند جداگانه) | — |
| فاز ۸ | مدیریت پروژه‌ها/درخواست‌ها، امتیازدهی و اعتماد، Matching هوشمند، داوری اختلاف، قرارداد دیجیتال | فاز پیش‌نیاز |

---

## ۲۱. پرامپت Claude Code — فاز ۱: زیرساخت پنل ادمین

```
در پروژه AAVAAN-site (Laravel 12.x / PHP 8.2.x — از ساختار و سینتکس استاندارد Laravel 12 استفاده کن،
نیازی به سازگاری با PHP 8.1 یا Laravel 10 نیست، این محدودیت قدیمی منسوخ شده):

۱. RBAC ادمین:
   - یک migration برای افزودن ستون nullable `admin_role` (enum: super_admin, support, finance,
     content_moderator) به جدول users — فقط برای کاربرانی با role=admin معنا دارد.
   - یک فایل config جدید `config/admin_permissions.php` که نگاشت هر admin_role به آرایه‌ای از
     کلیدهای بخش‌های مجاز پنل را نگه می‌دارد (مثلاً 'users', 'payments', 'content', 'tickets', ...).
   - Middleware «EnsureUserIsAdmin» (چک role===admin) و Middleware «CheckAdminPermission:{section}»
     (چک اینکه admin_role کاربر به بخش درخواستی دسترسی دارد) — هر دو روی گروه روت /admin اعمال شوند.
   - جدول و مدل `admin_activity_logs` (id, admin_user_id, action, subject_type, subject_id,
     description, ip_address, created_at) + یک trait یا helper ساده مثل `logAdminActivity()`
     که در فازهای بعدی از کنترلرها صدا زده می‌شود. در این فاز فقط یک صفحه‌ی فهرست ساده برایش بساز.

۲. Layout و اسکلت پنل:
   - `resources/views/admin/layouts/app.blade.php`: سایدبار + هدر، با هویت بصری آوان (پس‌زمینه
     عاجی #F6F1E7، لاجوردی #1F2A44 برای سایدبار/هدر، طلایی دودی #C9A24B برای تأکیدها، فونت
     YekanBakh برای عنوان‌ها و IRANSansX برای متن — همان فایل‌های فونت موجود در پروژه).
   - سایدبار شامل لینک به همه‌ی بخش‌های نهایی پنل (نمای کلی، کاربران، پلن‌ها، پرداخت‌ها، محتوا،
     گزارش‌ها، تیکتینگ، تنظیمات و ...) — بخش‌هایی که در این فاز ساخته نمی‌شوند به یک صفحه‌ی
     placeholder ساده‌ی «این بخش در فاز بعد تکمیل می‌شود» لینک شوند، نه ۴۰۴.
   - استفاده از Alpine.js برای باز/بسته‌شدن سایدبار در موبایل (مطابق همان الگوی پروژه).

۳. صفحه‌ی تنظیمات عمومی:
   - جدول `settings` (key string unique, value text, نوع: string/json).
   - فرم برای: اطلاعات تماس، لینک شبکه‌های اجتماعی، متاتگ‌های پیش‌فرض سئو، نرخ کارمزد پلتفرم
     (عدد درصدی، فعلاً بدون منطق مصرف — فقط ذخیره می‌شود).
   - **مهم:** هیچ کلید حساس درگاه پرداخت (زرین‌پال) در این فرم یا جدول قرار نگیرد؛ آن‌ها فقط در
     `.env` می‌مانند.

۴. نمای کلی (Dashboard):
   - مسیر `/admin` → AdminDashboardController@index.
   - کارت‌های آماری: تعداد کل کاربران، تعداد هنرمندان (role=artist)، تعداد کارفرمایان
     (role=production_team)، تعداد اشتراک‌های فعال، مجموع درآمد ماه جاری (از جدول پرداخت/تراکنش
     موجود پروژه — قبل از نوشتن کوئری، فایل‌های مدل پرداخت/اشتراک موجود را پیدا و بررسی کن تا
     اسم واقعی جدول/ستون‌ها را بشناسی، حدس نزن)، تعداد کاربران جدید امروز و این هفته.
   - هیچ کارتی برای «پروژه‌های فعال»، «درخواست‌های همکاری»، «استخدام‌های موفق» یا «نرخ تبدیل
     ثبت‌نام به همکاری» اضافه نکن — این مفاهیم هنوز در دیتابیس مدل نشده‌اند و ساختن کارت برایشان
     یعنی یا داده‌ی ساختگی نمایش داده شود یا کوئری روی جدول ناموجود خطا بدهد. به‌جایش یک کارت
     placeholder خاکستری با متن «به‌زودی» برای این سه مورد کافی‌ست.

۵. اسکلت جستجوی سراسری:
   - یک ورودی جستجو در هدر پنل که به `/admin/search?q=...` می‌رود.
   - یک کلاس `AdminSearchService` با یک متد `search(string $query): array` که فعلاً فقط روی
     جدول users (نام، ایمیل، موبایل) و جدول پرداخت/تراکنش (بر اساس شناسه) جستجو می‌کند، اما با
     ساختاری قابل‌توسعه (مثلاً آرایه‌ای از «provider»های جستجو) که در فازهای بعدی بدون بازنویسی
     کلاس بشود provider جدید (مثلاً پروژه) به آن اضافه کرد.

۶. کد باید کاملاً با PHP 8.2.x و Laravel 12.x سازگار باشد. قبل از تحویل، composer install واقعی
   (بدون ignore-platform-reqs) و یک migrate محلی روی sqlite اجرا و نتیجه را گزارش کن. همچنین فهرست
   کامل روت‌های /admin/* اضافه‌شده را با php artisan route:list خروجی بده.

۷. هیچ تغییری در فایل‌های خارج از این محدوده (مثلاً subscription.blade.php یا فایل‌های مربوط به
   پروفایل پیشرفته) ایجاد نکن مگر برای رفع تداخل مستقیم با این فاز.
```

---

## ۲۲. یادآوری برای مرحله‌ی حسابرسی (Claude، نه Claude Code)

بعد از فاز ۱:
- کلون برنچ، `composer install` واقعی بدون `--ignore-platform-reqs` (با پلتفرم PHP 8.2.x، نه 8.1)
- migrate محلی روی sqlite برای تست صحت migration جدید (`admin_role`, `admin_activity_logs`, `settings`)
- بررسی صریح که هیچ کوئری‌ای روی جدول/ستون ناموجود (پروژه، درخواست، استخدام) اجرا نمی‌شود
- بررسی middleware: یک کاربر غیرادمین واقعاً نباید بتواند به `/admin/*` دسترسی پیدا کند
- تأیید اینکه کلید درگاه پرداخت در هیچ فرم/جدول جدیدی expose نشده

فازهای ۲ تا ۸ هرکدام در یک پرامپت جداگانه نوشته می‌شوند، بعد از تأیید نتیجه‌ی فاز قبلی — طبق همان روال فعلی پروژه.
