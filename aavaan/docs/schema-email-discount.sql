-- ═══════════════════════════════════════════════════════════════════════════
-- آوان — فاز «فعال‌سازی حساب هنرمند با تأیید ایمیل + کد تخفیف/جشنواره»
-- DDL/DML مرجع MariaDB (نسخهٔ 11.8) این فاز.
--
-- منبعِ اجراییِ رسمی، مهاجرت‌های Laravel است:
--   database/migrations/2026_07_06_000001_backfill_email_verified_at_for_existing_users.php
-- روش اجرای توصیه‌شده در تولید: php8.5 artisan migrate --force
-- ═══════════════════════════════════════════════════════════════════════════

-- ─────────────────────────────────────────────────────────────────────────
-- ۱) تأیید ایمیل — بدون تغییر ساختار.
--    ستون users.email_verified_at از قبل در اسکیمای پایه وجود دارد
--    (مهاجرت 0001_01_01_000000_create_users_table). هیچ ستون جدیدی لازم نیست.
--
--    سازگاری با گذشته (حیاتی): برای اینکه هیچ کاربر فعلی قفل نشود، تاریخ تأیید
--    همهٔ کاربرانِ موجود (همهٔ نقش‌ها) پر می‌شود. فقط کاربران جدیدِ پس از این
--    تاریخ باید ایمیل خود را تأیید کنند (اجبار فقط روی نقش artist اعمال می‌شود).
-- ─────────────────────────────────────────────────────────────────────────
UPDATE `users`
    SET `email_verified_at` = NOW()
    WHERE `email_verified_at` IS NULL;

-- ─────────────────────────────────────────────────────────────────────────
-- ۲) کد تخفیف / کد جشنواره — بدون تغییر ساختار.
--    جدول‌های discount_codes و discount_code_uses از قبل موجودند
--    (مهاجرت‌های create_discount_codes_table و create_discount_code_uses_table).
--    این فاز فقط «مصرف‌کنندهٔ» کد را در جریان پرداخت اشتراک هنرمند اضافه می‌کند:
--      • اعتبارسنجی سمت سرور (endpoint JSON) — بدون سوزاندن کد.
--      • مصرف اتمیک هنگام موفقیتِ پرداخت با UPDATE شرطی روی used_count
--        (جلوگیری از race و oversell روی سقف استفاده)، معادلِ:
--
--        UPDATE `discount_codes`
--            SET `used_count` = `used_count` + 1
--            WHERE `id` = ?
--              AND `is_active` = 1
--              AND (`max_uses` IS NULL OR `used_count` < `max_uses`);
--
--        سپس درجِ یک ردیف در discount_code_uses (discount_code_id, user_id,
--        subscription_id, used_at).
--
--    هیچ ستون یا جدول جدیدی در این فاز اضافه نمی‌شود.
-- ─────────────────────────────────────────────────────────────────────────

-- ═══════════════════════════════════════════════════════════════════════════
-- پایان — این فاز فقط یک DML backfill دارد و هیچ تغییرِ ساختاری (DDL) ندارد.
-- ═══════════════════════════════════════════════════════════════════════════
